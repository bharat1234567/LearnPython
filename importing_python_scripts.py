import temp
print(temp.title) #this is how you import variable directly


# From this script call another script, give it some values it will perform addition and return the result.
from operations import add
a = 12
b = 32
sum = add(a,b)
print(sum)
